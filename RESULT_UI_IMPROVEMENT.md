# AI 면접 결과 페이지 UI/UX 개선안

> 대상 파일: `ai-interview-answer-result.vue`, `HexagonChart.vue`
> 작성일: 2026-03-15

---

## 1. 현재 구조 파악 및 문제점

### 현재 페이지 구성
```
헤더 (면접 완료 배지 + 타이틀)
  └─ 종합 평가 카드
       ├─ 종합 등급 (A/B/C/D/F 원형 배지)
       └─ 레이더 차트 + 점수 범례
  └─ AI 종합 총평 카드
       └─ 섹션별 파싱 (전반적인 인상 / 강점 / 개선점 / 최종 평가)
  └─ 질문별 상세 분석 카드 × N
       ├─ 당신의 답변
       ├─ AI 피드백
       └─ 개선 제안 (correction 필드 - 현재 plain text)
  └─ 액션 버튼 (녹화 다운로드 / PDF 저장 / 홈으로)
```

### 주요 문제점

| 영역 | 문제 |
|------|------|
| **질문 카드** | 질문 수가 많으면(6개+) 페이지가 너무 길어져 스크롤이 매우 많아짐 |
| **correction 렌더링** | `[이 질문의 핵심]`, `[잘한 점]`, `[이렇게 보완해보세요]`, `[표현 개선]` 텍스트가 `whiteSpace: pre-line`으로만 표시 → 서식이 없어 읽기 어려움 |
| **intent 필드 미사용** | 백엔드가 intent(질문 의도/역량 키워드)를 반환하지만 프론트에서 전혀 표시하지 않음 |
| **레이더 차트** | 색상이 단순 파란색, 앱 전반의 그린-블루 그라디언트 테마와 불일치. 애니메이션 없음 |
| **등급 기준 불명확** | A/B/C/D/F 등급만 표시, 총점/100점 환산 없이 사용자가 맥락 파악 어려움 |
| **점수 범례** | 작은 점 + 텍스트만. 점수가 높은지 낮은지 직관적으로 알기 어려움 |
| **로딩 상태 없음** | 데이터 불러오는 동안 빈 화면 또는 깨진 UI |
| **다시 도전 버튼 없음** | 결과를 보고 바로 재도전하려면 홈 → 선택 2단계를 거쳐야 함 |

---

## 2. 개선 아이디어

### 2-1. 질문 카드 → 아코디언(Accordion) 방식으로 전환

**현재**: 모든 질문이 펼쳐진 상태로 표시 → 스크롤이 끝없이 길어짐
**개선**: 기본적으로 접혀있고, 클릭하면 펼쳐지는 아코디언

```
Q1  자기소개 해주세요                               [펼치기 ▼]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Q2  React를 선택하신 이유가 무엇인가요?    ● 피드백 3개  [접기 ▲]
  ┌─ 당신의 답변 ─────────────────────────────────┐
  │  "저는 Vue보다 React의 생태계가..."             │
  └───────────────────────────────────────────────┘
  ┌─ AI 피드백 ────────────────────────────────────┐
  │  "답변 구조가 명확하고..."                      │
  └───────────────────────────────────────────────┘
  ┌─ 코칭 ─────────────────────────────────────────┐
  │  [이 질문의 핵심]  [잘한 점]  [보완]  [표현]    │
  └───────────────────────────────────────────────┘
```

**구현 포인트**
- 기본은 모두 닫힘
- 첫 번째 카드만 자동으로 열려있게 (진입 시 바로 내용 확인 가능)
- 헤더에 질문 유형 태그(자기소개 / 프로젝트 / 기술)와 피드백 수 배지 표시

---

### 2-2. correction 텍스트 파싱 → 구조화된 코칭 카드

**현재**: 백엔드가 반환하는 포맷된 텍스트를 `pre-line`으로만 표시
**개선**: 섹션별로 파싱해서 각기 다른 스타일의 카드로 렌더링

```
┌─ 코칭 리포트 ──────────────────────────────────────────┐
│                                                        │
│  🎯 이 질문의 핵심                                      │
│  ┌──────────────────────────────────────────────────┐  │
│  │  면접관이 확인하려는 것: 기술 선택의 주체적 판단력  │  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
│  ✅ 잘한 점                                             │
│  ┌──────────────────────────────────────────────────┐  │
│  │  생태계 규모와 커뮤니티를 언급한 점이 구체적       │  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
│  💡 이렇게 보완해보세요                                  │
│  • 실제로 Vue와 비교 검토했던 경험 언급                 │
│  • 트레이드오프(번들 사이즈, 러닝커브)도 인식하고        │
│    있다는 것을 보여주면 깊이가 생김                     │
│                                                        │
│  ✏️ 표현 개선                                           │
│  ❌ "React가 더 좋은 것 같아서"                         │
│  ⭕ "React의 방대한 생태계와 재사용성이 프로젝트        │
│      요구사항에 더 적합하다고 판단해"                   │
└────────────────────────────────────────────────────────┘
```

**구현 포인트**
- `parseCorrection(text)` 함수 작성: `[이 질문의 핵심]`, `[잘한 점]`, `[이렇게 보완해보세요]`, `[표현 개선]` 기준으로 split
- 각 섹션별 배경색/아이콘 매핑
- `[이렇게 보완해보세요]` → `•` bullet을 리스트 `<ul>` 태그로 변환
- `[표현 개선]` → ❌/⭕ 행을 감지해 before/after 카드로 렌더링

---

### 2-3. intent 필드 → 질문 카드 헤더에 역량 태그로 표시

**현재**: DB에 저장되어 있지만 UI에서 완전히 무시됨
**개선**: 각 질문 헤더에 역량 태그를 붙여서 "왜 이 질문을 했는지" 즉시 파악

```
┌─────────────────────────────────────────────────────┐
│  Q2   React를 선택하신 이유가 무엇인가요?            │
│       🏷 기술 이해도  🏷 설계 판단력                 │
└─────────────────────────────────────────────────────┘
```

---

### 2-4. 점수 범례 → 미니 진행 바로 교체

**현재**: `● 의사소통: 8점` 텍스트만
**개선**: 시각적 진행 바 + 색상 구간으로 강점/약점 즉시 파악

```
의사소통  ████████░░  8/10   🟢
개발역량  ██████░░░░  6/10   🟡
유연성    ████░░░░░░  4/10   🔴
```

- 8점 이상: 초록 (강점)
- 5~7점: 노랑 (보통)
- 4점 이하: 빨강 (집중 개선 필요)

---

### 2-5. 레이더 차트 개선

**현재 문제**: 단색 파란색, 앱 테마 불일치, 애니메이션 없음

**개선 사항**
```javascript
// HexagonChart.vue 수정 방향
datasets: [{
  // 테마 그라디언트 색상 적용
  borderColor: 'rgba(79, 156, 249, 1)',        // #4F9CF9
  backgroundColor: 'rgba(16, 185, 129, 0.15)', // #10B981 연하게
  pointBackgroundColor: '#4F9CF9',
  pointHoverBackgroundColor: '#10B981',

  // 진입 애니메이션
  // animation: { duration: 1000, easing: 'easeInOutQuart' }
}]

// 레이블에 점수 같이 표시
// "의사소통\n8점" 형식 → 차트 안에서 바로 확인 가능
```

---

### 2-6. 종합 등급 → 점수 환산 추가

**현재**: A/B/C/D/F 글자만
**개선**: 점수 환산 + 백분율 + 등급 의미 한눈에

```
┌──────────────────────┐
│    종합 등급           │
│                      │
│       A              │  ← 등급 (크게)
│    78 / 100          │  ← 점수 환산 (6개 항목 × 10점 → 100점 환산)
│   상위 15% 수준       │  ← 등급 설명
└──────────────────────┘
```

점수 환산 공식: `Math.round((총합 / 60) * 100)` → 6개 × 10점 = 60점 만점 → 100점 환산

---

### 2-7. 로딩 스켈레톤 추가

**현재**: 데이터 로드 중 빈 화면
**개선**: 페이지 구조와 동일한 스켈레톤 UI

```vue
<!-- 로딩 중일 때 -->
<div v-if="isLoading" class="skeleton-wrapper">
  <div class="skeleton-card" />  <!-- 종합 카드 placeholder -->
  <div class="skeleton-card" />  <!-- 총평 placeholder -->
  <div class="skeleton-card" v-for="i in 3" :key="i" /> <!-- Q카드 -->
</div>

<div v-else>
  <!-- 실제 내용 -->
</div>
```

---

### 2-8. 액션 버튼 개선

**현재**: 다운로드 / PDF / 홈으로
**개선**: 사용자가 결과를 보고 바로 다음 행동을 할 수 있도록

```
[📥 녹화 영상 저장]  [🖨 PDF 저장]  [🔄 다시 면접하기]  [🏠 홈으로]
```

- **다시 면접하기**: 같은 주제/경력으로 재도전 → `/ai-interview/select`로 이동 (현재 홈으로랑 동일하지만 의도가 명확해짐)
- **PDF 저장** 버튼: 현재 `window.print()` 방식인데 인쇄 다이얼로그가 뜨는 것 대신 "PDF로 저장" 안내 문구 추가

---

### 2-9. 탭 네비게이션 (선택적 대형 개선)

질문이 6개 이상일 때 전체 페이지가 매우 길어지는 근본 해결책

```
[📊 종합 요약] [📝 질문별 분석] [💡 개선 코칭 모음]
```

- **종합 요약 탭**: 등급 + 레이더 차트 + 총평
- **질문별 분석 탭**: Q&A 아코디언
- **개선 코칭 모음 탭**: 모든 질문의 `[이렇게 보완해보세요]` 섹션만 모아서 한 번에 보기

---

## 3. 우선순위 제안

| 우선순위 | 항목 | 난이도 | 임팩트 |
|----------|------|--------|--------|
| ⭐⭐⭐ | correction 파싱 → 구조화된 코칭 카드 (2-2) | 중 | 최고 |
| ⭐⭐⭐ | 질문 카드 아코디언 전환 (2-1) | 중 | 높음 |
| ⭐⭐ | intent 태그 표시 (2-3) | 낮음 | 중간 |
| ⭐⭐ | 점수 진행 바 (2-4) | 낮음 | 중간 |
| ⭐⭐ | 레이더 차트 테마 일치 (2-5) | 낮음 | 중간 |
| ⭐ | 로딩 스켈레톤 (2-7) | 중 | 중간 |
| ⭐ | 등급 점수 환산 (2-6) | 낮음 | 낮음 |
| ⭐ | 탭 네비게이션 (2-9) | 높음 | 높음 |

---

## 4. correction 파싱 함수 설계 (핵심 구현 가이드)

```javascript
// parseCorrection.js
const CORRECTION_SECTIONS = [
  { key: '핵심',  label: '이 질문의 핵심', icon: 'mdi-target',       color: '#3b82f6' },
  { key: '잘한',  label: '잘한 점',        icon: 'mdi-check-circle', color: '#10b981' },
  { key: '보완',  label: '이렇게 보완해보세요', icon: 'mdi-lightbulb', color: '#f59e0b' },
  { key: '표현',  label: '표현 개선',      icon: 'mdi-pencil',       color: '#8b5cf6' },
];

export function parseCorrection(text) {
  if (!text) return [];

  const sections = [];
  const sectionPattern = /\[(.+?)\]\n([\s\S]*?)(?=\[|$)/g;
  let match;

  while ((match = sectionPattern.exec(text)) !== null) {
    const title = match[1].trim();
    const content = match[2].trim();
    const meta = CORRECTION_SECTIONS.find(s => title.includes(s.key));

    sections.push({
      title,
      content,
      icon: meta?.icon || 'mdi-information',
      color: meta?.color || '#64748b',
      // 표현 개선 섹션은 ❌/⭕ 행 파싱
      expressions: title.includes('표현') ? parseExpressions(content) : null,
      // 보완 섹션은 bullet 파싱
      bullets: title.includes('보완') ? parseBullets(content) : null,
    });
  }

  return sections;
}

function parseExpressions(text) {
  // "❌ 원문 → ⭕ 개선문" 또는 "❌ 원문\n⭕ 개선문" 파싱
  const pairs = [];
  const lines = text.split('\n');
  let before = null;

  lines.forEach(line => {
    if (line.startsWith('❌')) before = line.replace('❌', '').trim();
    else if (line.startsWith('⭕') && before) {
      pairs.push({ before, after: line.replace('⭕', '').trim() });
      before = null;
    }
  });

  return pairs;
}

function parseBullets(text) {
  return text.split('\n')
    .map(l => l.replace(/^[•·\-]\s*/, '').trim())
    .filter(Boolean);
}
```

---

## 5. 전체 개선 후 페이지 구조 (목표)

```
헤더
  └─ 종합 평가 카드
       ├─ 점수 환산 등급 (78/100 · A등급)
       ├─ 레이더 차트 (그라디언트 테마)
       └─ 점수 진행 바 범례
  └─ AI 종합 총평 카드 (기존 유지)
  └─ 질문별 상세 분석 (아코디언)
       └─ Q카드 (헤더: 질문 + intent 태그)
            ├─ 당신의 답변
            ├─ AI 피드백
            └─ 코칭 리포트 (4섹션 구조화 카드)
                 ├─ 🎯 이 질문의 핵심
                 ├─ ✅ 잘한 점
                 ├─ 💡 이렇게 보완해보세요 (bullet list)
                 └─ ✏️ 표현 개선 (before/after 카드)
  └─ 액션 버튼 (PDF / 녹화 / 다시 도전 / 홈)
```
